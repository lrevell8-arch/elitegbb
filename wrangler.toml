name = "elitegbb-app"
compatibility_date = "2024-01-01"

[site]
bucket = "./frontend/build"

# Cloudflare Pages build configuration
[build]
command = "cd frontend && DISABLE_ESLINT_PLUGIN=true npm run build"

# Pages build output directory - required for Cloudflare Pages
pages_build_output_dir = "frontend/build"
